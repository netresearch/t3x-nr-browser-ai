page = PAGE
page {
    10 = CONTENT
    10 {
        table = tt_content
        select {
            orderBy = sorting
            where = colPos=0
        }
    }
}

tt_content.text = TEXT
tt_content.text {
    value = <p data-fallback-output>Rendered fallback content</p>
}
